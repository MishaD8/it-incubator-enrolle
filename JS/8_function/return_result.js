// возврат результата
function calcSumm(numOne, numTwo) {
    let numSumm = numOne + numTwo;

    //возврат
    return numSumm;

    //дальше код не выполняется
}
let funcRezult = calcSumm(1, 2);

console.log(`сумма: ${funcRezult}`); //сумма: 3