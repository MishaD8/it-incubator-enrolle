// свойства объектов. Ключ + значение

let userInfo = {
    name: "Olya", // свойство объекта
    age: 22, // последняя (висячая) запятая. Благодаря ей можно перемещать линию куда угодно и не будет ошибки
};

console.log(userInfo);
console.log(userInfo.name);
